

Go to the correct folder (IMPORTANT)

Your code is inside data storage.
cd "C:\Users\Anuj\Desktop\mhab\MHAB-GitHUB\data storage"


Verify:

dir

You must see:

initialisation.cpp
capture.cpp
writer.cpp
db.cpp
third_party

(Optional but recommended) Clean old outputs

This ensures a fresh run every time.

del *.obj
del initialisation.exe
rmdir /s /q archive


(If it says "file not found", that's fine.)

Compile everything (REAL SQLITE, NO STUB)

This is the exact compile command you should always use:

cl /std:c++17 /DUSE_SQLITE=1 initialisation.cpp capture.cpp writer.cpp db.cpp third_party\sqlite\sqlite3.c /EHsc


Expected result:

No errors

initialisation.exe created

Run the program
initialisation.exe


You should see:

Press Enter to stop capture...
[DB] Thread started (SQLite).
[Writer] Thread started.
[Capture] Thread started...

Let it run for ~5 seconds

Just wait. Frames will be generated.

Stop the program CLEANLY

Press ENTER
Do NOT press Ctrl+C

You should see:

Stopping threads...
[Writer] Thread exiting.
[DB] Thread exiting.
[Capture] Thread exiting.
Shutdown complete.

Verify output files exist
dir archive


You should see:

frames.sqlite3
frame_<timestamp>.raw16.lz4
frame_<timestamp>.raw16.lz4
...

Verify database rows (NO SQLITE INSTALL NEEDED)

Run this exact command:

python -c "import sqlite3; conn=sqlite3.connect(r'archive\frames.sqlite3'); print('rows =', conn.execute('select count(*) from frames').fetchone()); print(conn.execute('select ts_ns, filepath from frames order by ts_ns desc limit 3').fetchall()); conn.close()"


Expected output:

rows = (8,)
[(timestamp, 'archive\\frame_xxx.raw16.lz4'), ...]


This proves DB + files are correct.